{
  "name": "wp-seen-posts",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js && php tests/public-counts.php && php tests/gamification.php && php tests/top-seen-widget.php && php tests/analytics.php && node --check assets/js/early-hide.js && node --check assets/js/seen-posts.js && node --check assets/js/single-post.js && node --check assets/js/public-counts.js && node --check assets/js/gamification.js && node --check assets/js/analytics.js && php -l wp-seen-posts.php && php -l includes/class-settings.php && php -l includes/class-public-counts.php && php -l includes/class-gamification.php && php -l includes/class-top-seen-widget.php && php -l includes/class-analytics.php && php -l includes/functions.php"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
