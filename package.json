{
  "name": "wp-seen-posts",
  "version": "1.0.22",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js && node --check assets/js/early-hide.js && node --check assets/js/seen-posts.js && node --check assets/js/single-post.js"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
